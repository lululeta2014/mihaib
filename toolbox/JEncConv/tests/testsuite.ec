# Run with incorrect arguments
BadArgs

# Basic usage
Basic

# Ensure program doesn't overwrite any existing files
# without the -f/--force option
Overwrite

# Command line use of plugins
CmdLinePlugins

# Tests for some of the plugins
Plugins
