Basic polymer tests & examples

init-fields/	Check what happens the first time fiels are set,
		via defaults, ready(), or html attributes. Particularly if
		the mypropertyChanged watchers run.
avoid-get-set/	Avoid getters and setters, it seems this isn't “polymeric”.
